{
  "name": "botforge",
  "version": "1.0.0",
  "description": "Panel d'hébergement de bots Discord h24 avec PM2",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "setup": "npm install && npm install -g pm2 && pm2 startup"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
